[package]
name = "game"
version = "0.1.0"
authors = ["djade <djadenkus@gmail.com>"]
edition = "2018"

[dependencies]
engine = { path = "../engine" }
serde = { version = "1.0.114", features = ["derive"] }
ron = "0.6.0"
rand = "0.7"
